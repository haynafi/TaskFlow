version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=your-super-secret-key-change-this-in-production
    volumes:
      - ./data:/app/data
    restart: unless-stopped